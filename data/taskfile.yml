# https://taskfile.dev

version: '3'

vars:
  BIN: gonuxt

tasks:
  default:
    cmds:
      - task: build
      - task: run
  build:
    cmds:
      - CGO_ENABLED=0 go build -o {{.BIN}}
  run:
    cmds:
      - ./{{.BIN}}
